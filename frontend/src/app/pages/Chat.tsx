import AppLayout from "../components/common/AppLayout";
import ChatContainer from "../containers/Chat";

const Chat = () => (
  <AppLayout>
    <ChatContainer />
  </AppLayout>
);

export default Chat;
